# CI configuration

language: cpp

sudo: false

dist: bionic

python: "3.6"

matrix:
  include:
    - compiler: gcc
      addons:
        apt:
          update: true
          sources:
            - sourceline: 'ppa:hnakamur/boost'
          packages:
            - gcc-8
            - libboost1.67-dev

    - compiler: clang
      addons:
        apt:
          update: true
          sources:
            - sourceline: 'ppa:hnakamur/boost'
          packages:
            - clang-9
            - libboost1.67-dev


before_script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_BUILD_TYPE=Release ..

script: make && make test
